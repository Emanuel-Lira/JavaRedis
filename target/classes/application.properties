spring.application.name=produtoscache
# CONFIGURA��O DO BANCO DE DADOS MYSQL
# ===============================================
# URL de conex�o para MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/produtosdb?useSSL=false&allowPublicKeyRetrieval=true
# Usu�rio e senha definidos no docker-compose.yml (no MySQL 8, o usu�rio padr�o � 'root')
spring.datasource.username=root
spring.datasource.password=root

# ===============================================
# CONFIGURA��O DO JPA / HIBERNATE
# ===============================================
# Define o "dialeto" do SQL que o Hibernate vai usar para se comunicar com o MySQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ddl-auto=update: O Hibernate tentar� atualizar o schema do banco com base nas suas entidades.
spring.jpa.hibernate.ddl-auto=update

# ===============================================
# CONFIGURA��O DO REDIS (n�o muda)
# ===============================================
spring.redis.host=localhost
spring.redis.port=6379

# ===============================================
# CONFIGURA��O DO CACHE (n�o muda)
# ===============================================
spring.cache.type=redis
spring.cache.cache-names=produtos
spring.cache.redis.time-to-live=-1
